
server:
  port: 8888

logging:
  level:
    root: info

spring:
  security:
    basic:
      enabled: true
    user:
      name: user
      password: user123
  servlet:
    multipart:
      max-file-size: 512MB
      max-request-size: 512MB
      enabled: true
      file-size-threshold: 0

fileserver:
   home: /data/fileserver/

